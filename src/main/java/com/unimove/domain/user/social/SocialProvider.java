package com.unimove.domain.user.social;

/**
 * Provedores de login social aceitos. Apple entra quando houver app iOS na
 * App Store (que exige "Sign in with Apple" se qualquer outro social existir).
 */
public enum SocialProvider {
    GOOGLE
}
